version: "3.5"
services:
    postgres:
        image: postgres:9.5
        container_name: operations_postgres
        environment:
            - POSTGRES_USER=postgres
            - POSTGRES_PASSWORD=postgres
        ports:
            - "5432:5432"
        networks:
            - operations
        volumes:
            - postgres:/data/db
    redis:
        image: redis
        container_name: operations_redis
        ports:
            - "6379:6379"
        networks:
            - operations
        volumes:
            - redis:/data

networks:
    operations:
        name: operations-network
        internal: true

volumes:
    postgres:
        driver: local
    redis:
        driver: local
